#include<stdio.h>

int main()
{
    // int a=10;
    // float a=5.12;    error aauxa     but
    struct date{
        int date;
        char name[20];
    }a;//strure ko name rw int ko name same keii farak pardainw actually a.date=2004; garda date is member of object 'a' ani tuppo ma lekheko date means class
    return 0;
}